<h1>Listing Users</h1>
 
<table>
  <tr>
    <th>Email</th>
    <th>Last Signed in at</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.last_sign_in_at %></td>
    </tr>
  <% end %>
</table>