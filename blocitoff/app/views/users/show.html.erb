<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
 
<p>
  <strong>Last signed in at:</strong>
  <%= @user.last_sign_in_at %>
</p>

<h2>Items</h2>
<%= render @user.items %>

<h2>Add an Item:</h2>
<%= render 'items/form' %>

<%= link_to 'Back', users_path %>